#include <stdio.h>

int main() {
    FILE *fp;
    char ch;
    int count = 0;

    fp = fopen("student.txt", "r");

    while ((ch = fgetc(fp)) != EOF) {
        if (ch == '\n')
            count++;
    }

    fclose(fp);
    printf("Number of records = %d", count + 1);
    return 0;
}
