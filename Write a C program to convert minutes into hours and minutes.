#include <stdio.h>

int main() {
    int minutes, hours, remMinutes;

    printf("Enter total minutes: ");
    scanf("%d", &minutes);

    hours = minutes / 60;
    remMinutes = minutes % 60;

    printf("Hours = %d\nMinutes = %d", hours, remMinutes);

    return 0;
}
